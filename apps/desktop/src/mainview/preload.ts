/**
 * Preload script for the main view
 *
 * This runs before the webview loads and provides a secure bridge
 * between the main process and the renderer
 */

// Export any APIs that should be available to the renderer
export {};
